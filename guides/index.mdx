# Guides

Coming soon.

## Testing Code rendering

### Live code

```jsx live
function functionName(arg1) {
	return (
		<Box padding={3}>
			<Box
				as="ul"
				padding={3}
				font="body"
				background="page"
				theme="dark"
				border
				rounded
			>
				<li>
					<TextLink href="#testing">Something linked</TextLink>
				</li>
			</Box>
			<Text>
				Really long text to break the flow Cum sociis natoque penatibus et
				magnis dis parturient montes, nascetur ridiculus mus. Vestibulum id
				ligula porta felis euismod semper. Nulla vitae elit libero, a pharetra
				augue. Lorem ipsum dolor sit amet, consectetur adipiscing elit.
			</Text>
		</Box>
	);
}
```

```ts
const tryBlue = (color?: string) => color ?? 'blue';
const col = tryBlue('pink');

// This does some really important stuff
function conflagate(amount: number) {
	if (amount > 22) {
		return Math.floor(3.14159);
	} else {
		console.log('it\'s not enough');
		throw new Error('Kaboooom!!!')
	}
}

export default const cheese = 'burger';

```

```html
<!DOCTYPE html>
<body>
	<main class="main-content">
		<h1>Something important</h1>
		<a href="/home">Go home</a>
	</main>
</body>
```

```jsx live
<Box padding={3}>
	<Box
		as="ul"
		padding={3}
		font="body"
		background="page"
		theme="dark"
		border
		rounded
	>
		<li>
			<TextLink href="#testing">Something linked</TextLink>
		</li>
	</Box>
</Box>
```

### JSX rendering

```jsx
<Box padding={{ md: 2 }}>
	<Heading>Heading text</Heading>
</Box>
```

### JSON rendering

```json
{
	"data": [
		{ "key": "special-string", "amount": 33 },
		{ "key": "normal-string", "amount": 14 }
	]
}
```
