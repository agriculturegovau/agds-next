---
date: 18th July 2024
title: AgDS Beta v1.23.1 release
description: Fixes various components for React v16 consumers and AppLayoutSidebar level two item backgrounds.
---

## Updates

### [App layout](/components/app-layout)

- `AppLayoutSidebar`: Fix level two items not inheriting `body` background.

### [Checkbox](/components/checkbox)

- Use `useId` from core, rather than React for backwards compatibility.

### [Control group](/components/control-group)

- Use `useId` from core, rather than React for backwards compatibility.

### [Pagination](/components/pagination)

- `PaginationButtons`: Use `useId` from core, rather than React for backwards compatibility.

### [Radio](/components/radio)

- Use `useId` from core, rather than React for backwards compatibility.

## Released packages

```sh
"@ag.ds-next/react": "1.23.1"
```

## Full changelog

Aside from the complete release notes on the @ag.ds-next website, you can also view the verbose change log in the [related PR](https://github.com/agriculturegovau/agds-next/pull/1718) for this release.
