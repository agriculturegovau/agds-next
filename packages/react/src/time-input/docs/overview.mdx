---
title: Time input
description: The Time input component allows users to enter an exact time in various formats.
group: Forms
storybookPath: /story/Forms-TimeInput--basic
unreleased: true
figmaGalleryNodeId: REPLACE_ME
---

```jsx live
() => {
	// Set the value to a value that the user might think is valid
	const [value, setValue] = React.useState('9:66');

	const onChange = (e) => {
		console.log(`onChange playroom`, e);
		setValue(e);
	};

	const invalid = !isValidTime(value);

	console.group('playroom');
	console.log(`invalid`, invalid);
	console.log(`value`, value);
	console.groupEnd('playroom');

	return (
		<TimeInput
			invalid={invalid}
			label="Time"
			message="Enter a valid time"
			onChange={onChange}
			value={value}
		/>
	);
};
```

```jsx live
<TimeInput />
```

**High-level introduction to the component. This paragraph should only be a few sentences long.**

<DoHeading />

- use this component to…
- aim for 3-5 bullet points here

<DontHeading />

- use this component in this way…
- aim for 3-5 bullet points here

## Name of variant or feature

**Describe the variant or feature here. You can use code examples and multiple parapgraphs if you wish.**

```jsx live
<TimeInput variant="one" label="Time" />
```

## Name of variant or feature

**Describe the variant or feature here. You can use code examples and multiple parapgraphs if you wish.**

```jsx live
<TimeInput variant="one" label="Time" />
```
