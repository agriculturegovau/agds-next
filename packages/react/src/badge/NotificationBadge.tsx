import { NotificationBadge as AgDsNotificationBadge } from '../notification-badge';

/**
 * @deprecated Use `NotificationBadge` from the `notification-badge` entrypoint. Example: `import { NotificationBadge } from '@ag.ds-next/react/notification-badge'`"
 */
export const NotificationBadge = AgDsNotificationBadge;
