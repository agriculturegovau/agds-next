import { StatusBadge as AgDsStatusBadge } from '../status-badge';

/**
 * @deprecated Use `StatusBadge` from the `status-badge` entrypoint. Example: `import { StatusBadge } from '@ag.ds-next/react/status-badge'`"
 */
export const StatusBadge = AgDsStatusBadge;
