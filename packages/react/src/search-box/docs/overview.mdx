---
title: Search box
description: An input that allows users to enter a keyword to search a website for specific content.
group: Forms
storybookPath: /story/forms-searchbox--basic
figmaGalleryNodeId: 12444%3A100493
relatedComponents: ['search-input']
---

```jsx live
<SearchBox onSubmit={console.log}>
	<SearchBoxInput label="Search this website" />
	<SearchBoxButton>Search</SearchBoxButton>
</SearchBox>
```

Use the `SearchBox` component to help users find content or pages through keywords. This can be particularly useful for a web service with a large amount of pages.

<DoHeading />

- use to allow users to search for and discover content
- include placeholder text to explain what the box is for
- use to search for content on a server
- allow users to clear the input and remove the current search term.

<DontHeading />

- use to filter information that has loaded – use [SearchInput](/components/search-input)
- include a label when used in [Header](/components/header)
- display the active search term as a filter when used with [Search filters](/patterns/search-filters)
- indicate that the field is optional.

## Responsive icon

Use the `iconOnly` prop to configure at which breakpoints the search icon should replace the button label. This allows more space for text to be written in the text input.

```jsx live
<SearchBox onSubmit={console.log}>
	<SearchBoxInput label="Search this website" />
	<SearchBoxButton iconOnly={{ xs: true, md: false }}>Search</SearchBoxButton>
</SearchBox>
```

## Clear button

Users can use the ‘Clear search’ button to the right of the input field to remove the current search term.

```jsx live
() => {
	const [value, setValue] = React.useState('Oranges');
	return (
		<SearchBox onSubmit={console.log}>
			<SearchBoxInput
				label="Search this website"
				onChange={(e) => setValue(e.target.value)}
				value={value}
			/>
			<SearchBoxButton>Search</SearchBoxButton>
		</SearchBox>
	);
};
```

## Colour

```jsx live colorSection
<Stack gap={1.5}>
	<Box maxWidth="32rem">
		<SearchBox onSubmit={console.log}>
			<SearchBoxInput
				label="Search box: icon search button on [%_PALETTE_%] background"
				labelVisible
			/>
			<SearchBoxButton iconOnly={true}>Search</SearchBoxButton>
		</SearchBox>
	</Box>
	<SearchBox onSubmit={console.log}>
		<SearchBoxInput
			label="Search box: icon search button on [%_PALETTE_%] background"
			labelVisible
			defaultValue="Oranges"
		/>
		<SearchBoxButton iconOnly={{ xs: true, md: false }}>Search</SearchBoxButton>
	</SearchBox>
</Stack>
```
