import { Fragment } from 'react';
import { createIcon } from '../Icon';

// Please add icon SVG code below, excluding the <svg> tag.
export const {{iconName}} = createIcon(
	<Fragment>
		<path d="M12 5V19" />
		<path d="M5 12H19" />
	</Fragment>,
	'{{iconName}}'
);
